import React from 'react';
import ReactDOM from 'react-dom';
import Whiteboard from './Whiteboard';

const root = document.createElement('div');
document.body.appendChild(root);

ReactDOM.render(<Whiteboard />, root);
